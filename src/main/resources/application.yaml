spring:
    datasource:
        username: root
        url: jdbc:mysql://localhost:3306/products
        password: root

    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: true
